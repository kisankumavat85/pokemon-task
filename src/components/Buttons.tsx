"use client";

import { Button } from "@/components/ui/button";
import React from "react";

const Buttons = () => {
  return (
    <div>
      <Button>Prev</Button>
      <Button>Next</Button>
    </div>
  );
};

export default Buttons;
